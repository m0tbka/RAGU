# HTTP-сервис RAGU

---
1. [Что это](#что-это)
2. [Запуск](#запуск)
3. [Конфигурация](#конфигурация)
4. [Каталог графов](#каталог-графов)
5. [Ingestion](#ingestion)
6. [Эндпоинты поиска](#эндпоинты-поиска)
7. [Health и readiness](#health-и-readiness)
8. [Ошибки](#ошибки)
9. [Проектные решения](#проектные-решения)
10. [Логирование](#логирование)
11. [Текущие ограничения](#текущие-ограничения)

---

## Что это

`ragu.api` — FastAPI-сервис, который выставляет заранее построенный граф знаний
по HTTP. Он поставляется внутри пакета, поэтому установленный RAGU умеет
обслуживать граф без клона репозитория, а клиенты на любом языке могут делать
запросы вместо того, чтобы импортировать RAGU как python-библиотеку.

Сервис **обслуживает** граф и никогда его не строит. Сначала постройте граф —
`KnowledgeGraph.build_from_docs` или один из скриптов в `examples/` — затем
укажите сервису эту папку хранилища.

Один процесс владеет одним графом и выставляет по маршруту на каждый режим
поиска, чтобы шлюз мог назначить им разные таймауты и лимиты. Это важно:
глобальный поиск оценивает каждое саммари сообщества отдельным LLM-вызовом
(N+1 вызовов на N сообществ), а локальный и наивный тратят по одной генерации.

Установка с extra `api`:

```bash
pip install -e ".[api]"
```

## Запуск

```bash
# Заготовленные ответы, без графа и без LLM — для разработки клиентов
python -m ragu.api --backend stub

# Реальный граф
python -m ragu.api --backend ragu --storage-folder ragu_working_dir
```

`--host`, `--port`, `--backend` и `--storage-folder` переопределяют
соответствующие переменные окружения из командной строки. Интерактивная
документация — на `/docs`.

В Docker `docker compose up -d ragu-api` собирает образ из корня репозитория и
монтирует готовый граф в `/data/graph`. Детали развёртывания, на которых легко
обжечься — whitelist в `.dockerignore`, вшитые словари tiktoken, стартовый
период healthcheck и права записи для uid 1000 — описаны в
[`ragu/api/README.md`](../../ragu/api/README.md).

## Конфигурация

Настройки сервиса — переменные окружения `RAGU_API_*`, которые читает
`ServiceSettings` (`ragu/api/config.py`), при желании из файла `.env`.

| Переменная | По умолчанию | Значение |
|---|---|---|
| `RAGU_API_GRAPHS` | — | Графы для обслуживания, JSON-список. Без неё — один граф из плоских переменных |
| `RAGU_API_BACKEND` | `ragu` | `ragu` загружает реальный граф, `stub` отдаёт заготовленные ответы |
| `RAGU_API_HOST` | `127.0.0.1` | Адрес прослушивания; образ передаёт `--host 0.0.0.0` сам |
| `RAGU_API_PORT` | `8020` | Порт |
| `RAGU_API_STORAGE_FOLDER` | `ragu_working_dir` | Папка с построенным графом |
| `RAGU_API_LANGUAGE` | `russian` | Передаётся в `Settings.language` |
| `RAGU_API_SETTINGS_FILE` | — | JSON с `Settings`, сохранённый при сборке, загружается вместо умолчаний |
| `RAGU_API_EMBEDDER_DIM` | — | Размерность эмбеддингов; без неё определяется пробным запросом |
| `RAGU_API_RATE_MIN_DELAY` | — | Минимальная задержка между LLM-вызовами, секунды |
| `RAGU_API_RATE_MAX_SIMULTANEOUS` | — | Максимум одновременных LLM-вызовов |
| `RAGU_API_LLM_CACHE` | — | Путь к кэшу ответов LLM; без него кэш выключен |
| `RAGU_API_RERANK_TIMEOUT` | `10` | Сколько секунд ждать реранкер, прежде чем ответить без него |
| `RAGU_API_ENGINE_CACHE_SIZE` | `32` | Сколько движков (режим, язык) держать построенными |
| `RAGU_API_MAX_BATCH_SIZE` | `50` | Максимум запросов, принимаемых маршрутом /batch |
| `RAGU_API_MAX_TOP_K` | `100` | Потолок для клиентских `top_k` / `rerank_top_k` |
| `RAGU_API_MIN_CLUSTER_SIZE_FLOOR` | `1` | Нижняя граница для `min_cluster_size` глобального поиска |
| `RAGU_API_STUB_MISSING_CAPABILITIES` | — | Только для стаба: какие возможности объявлять отсутствующими. Неизвестное имя роняет старт, а не симулирует пустоту |

`RAGU_API_STORAGE_FOLDER` должна указывать на одну папку графа, а не на папку с
несколькими, и эта папка обязана существовать и быть непустой — иначе сервис
откажется стартовать, а не создаст её.

Учётные данные LLM и эмбеддера берутся из того же `.env`, что и остальной RAGU
(`ragu.common.env.Env`): `LLM_MODEL_NAME`, `LLM_BASE_URL`, `LLM_API_KEY`, и
опционально `EMBEDDER_BASE_URL`, `EMBEDDER_API_KEY`, `EMBEDDER_MODEL_NAME`.

## Каталог графов

Один процесс обслуживает несколько графов. У каждого есть имя, и это имя —
сегмент пути:

| Маршрут | Что возвращает |
|---|---|
| `GET /v1/graphs` | все графы с размерами и доступными режимами |
| `GET /v1/graphs/{id}` | один граф |
| `GET /v1/graphs/{id}/capabilities` | какие режимы доступны и почему недоступны остальные |

```json
{"default": "books",
 "graphs": [{"id": "books", "loaded": true, "language": "russian",
             "stats": {"entities": 128400, "relations": 170233,
                       "chunks": 9812, "community_summaries": 341},
             "modes": [{"mode": "local", "available": true,
                        "missing_capability": null, "reason": null},
                       {"mode": "global", "available": false,
                        "missing_capability": "community_summaries",
                        "reason": "This graph carries no community summaries..."}],
             "error": null}]}
```

`capabilities` существует, чтобы клиент гасил недоступные корпусу режимы в
интерфейсе, а не предлагал их и читал 409.

Каталог задаётся переменной `RAGU_API_GRAPHS` — список в JSON:

```bash
RAGU_API_GRAPHS='[{"id":"books","storage_folder":"/data/books","language":"russian"},
                  {"id":"papers","storage_folder":"/data/papers","language":"english",
                   "embedder_dim":1024}]'
```

В каждой записи обязательны `id` и `storage_folder`, опциональны `language`,
`settings_file` и `embedder_dim`. Если переменная не задана, из плоских
`RAGU_API_STORAGE_FOLDER` / `RAGU_API_LANGUAGE` строится один граф с именем
`default` — развёртыванию с одним графом каталог не нужен.

Граф, который не загрузился, записывается и пропускается: остальные продолжают
обслуживаться, а `/v1/graphs` сообщает, почему этого нет.

### Как несколько графов уживаются в одном процессе

`Settings` — синглтон уровня процесса, и `Index` читает
`Settings.storage_folder` в конструкторе. Читает **только** там, и эмбеддер с
движками так же читают лимиты токенов и имена токенизаторов в момент
конструирования — поэтому реестр строит графы по одному, применяет настройки
графа вокруг конструирования и откатывает синглтон обратно.

Движки, построенные позже — по одному на язык, по требованию, — иначе подхватили
бы то, что к этому моменту лежит в синглтоне, поэтому бэкенд снимает лимит
контекста и токенизатор в момент загрузки и передаёт их дальше явно.

## Ingestion

Сборка графа занимает от минут до часов, и никакой запрос столько не держится,
поэтому документы отправляются задачей, а задача опрашивается.

| Маршрут | |
|---|---|
| `POST /v1/graphs/{id}/documents` | `202` с задачей; `Location` указывает на неё |
| `GET /v1/jobs` | все задачи процесса, новые первыми (`?graph_id=`) |
| `GET /v1/jobs/{job_id}` | одна задача |
| `DELETE /v1/jobs/{job_id}` | попросить работающую задачу остановиться |

Передавайте `Idempotency-Key`, чтобы ретрай клиента не собрал один и тот же
корпус дважды.

**Поиск по графу отвечает `409 GRAPH_BUSY`, пока граф собирается.**
`build_from_docs` пишет в те же хранилища, из которых читает поиск, а
файловые не переживают конкурентного доступа, поэтому запись побеждает, а
чтению говорят вернуться позже.

Ingestion выключен по умолчанию. Граф принимает документы, только если это
написано в его спеке: обслуживание готового графа и его сборка — разные
нагрузки с разной конфигурацией.

```json
{"id": "corpus", "storage_folder": "/data/corpus",
 "build": {"enabled": true, "chunker": "simple", "chunk_size": 1200,
           "chunk_overlap": 100, "vector_only": false,
           "make_community_summary": true}}
```

`vector_only` строит только векторы чанков и пропускает извлечение сущностей —
единственный режим, работающий без экстрактора, и он же оставляет `local` и
`global` недоступными.

Задачи живут в памяти: одна реплика, рестарт теряет то, что выполнялось.
Хранилище задач — интерфейс, чтобы его можно было заменить общим; пока это
намеренно не сделано.

## Эндпоинты поиска

Четыре режима — `global`, `local`, `naive`, `mix` — каждый в четырёх формах:

Все маршруты поиска живут внутри графа. Канонический —
`POST /v1/graphs/{id}/search/{mode}`; плоский `POST /v1/search/{mode}` оставлен
для клиентов, написанных до того, как сервис стал обслуживать больше одного
графа, и адресует граф по умолчанию.

| Маршрут | Что возвращает |
|---|---|
| `POST /v1/graphs/{id}/search/{mode}` | один ответ с источниками |
| `POST /v1/graphs/{id}/search/{mode}/retrieve` | только контекст, без генерации |
| `POST /v1/graphs/{id}/search/{mode}/batch` | по ответу на каждый запрос из `queries` |
| `POST /v1/graphs/{id}/search/{mode}/stream` | ответ через Server-Sent Events |

`mix` ансамблирует локальный и наивный движки. Параметры дочерних движков
названы отдельно — `local_params` и `naive_params` — потому что
`MixSearchEngine` читает их из конструктора: `batch_search` игнорирует свой
аргумент `params` целиком, а `batch_query` берёт из него только
`ensemble_responses`.

| Режим | Тело |
|---|---|
| `global` | `query`, `params: GlobalSearchParams` |
| `local` | `query`, `use_query_plan=true`, `params: LocalParams` |
| `naive` | `query`, `use_query_plan=true`, `params: NaiveSearchParams` |
| `mix` | `query`, `use_query_plan=true`, `params: MixQueryParams`, `local_params`, `naive_params` |

`params` — это собственный класс параметров движка, встроенный в модель
запроса, а не переписанный поле за полем:

```json
{
  "query": "кто написал роман?",
  "use_query_plan": true,
  "params": {"top_k": 20, "rerank_top_k": null, "use_summary": true, "use_chunks": true}
}
```

Три следствия этого выбора:

- **Умолчания — движка.** Без `params` берутся `LocalParams()` /
  `NaiveSearchParams()`, то есть `use_summary` равен `false`, а `top_k` — `20`.
  Клиенту, которому это важно, следует передавать значения явно.
- **Границы — сервиса.** У датаклассов их нет, поэтому `top_k` и `rerank_top_k`
  ограничиваются сверху значением `RAGU_API_MAX_TOP_K`, а `min_cluster_size`
  поднимается до `RAGU_API_MIN_CLUSTER_SIZE_FLOOR`.
- **Неизвестные поля отвергаются — и снаружи `params`, и внутри.** И `top_k` на
  верхнем уровне (форма до появления `params`), и опечатка `params.topk` дают
  `400`, а не тихо обслуженное умолчание движка.

Глобальный поиск не принимает `use_query_plan`: его ответ синтезируется из
саммари, которые и так покрывают весь корпус, поэтому декомпозиция вопроса
привела бы лишь к повторной оценке тех же саммари на каждый подзапрос. Поле
`used_query_plan` остаётся в ответе и для этого режима всегда `false`.

Успешный ответ:

```json
{
  "query": "Кто написал роман 'Камо грядеши'?",
  "mode": "local",
  "used_query_plan": true,
  "answer": "Роман написал Генрик Сенкевич...",
  "sources": [{"id": "chunk-8f3...", "type": "chunk", "content": "...", "score": 0.87}],
  "subqueries": [{"query": "Кто написал роман?", "answer": "Генрик Сенкевич"}]
}
```

`sources` — это ретривал, разложенный в плоские `{id, type, content, score}`:
чанки и их скоры из `NaiveSearchResult`, сущности / связи / саммари / чанки из
`LocalSearchResult`, оценённые инсайты из `GlobalSearchResult`. Тип результата,
который сервис не моделирует, вырождается в один источник, отрендеренный через
`to_text()`.

Если отработал план запроса, `sources` несёт свидетельства **всех** подзапросов
без дубликатов, а не только финального: промежуточные ответы возвращаются в
`subqueries`, значит и их доказательная база возвращается вместе с ними.


### Язык ответа

Каждый запрос, порождающий ответ, принимает необязательный `language`; без него
применяется умолчание сервиса (`RAGU_API_LANGUAGE`).

Это параметр запроса, а не настройка развёртывания, потому что движки
фиксируют язык в момент конструирования — каждый читает его при рендере
промпта ответа, — и язык, заданный на старте, означает, что русский вопрос к
англоязычному корпусу будет отвечен по-английски. Поэтому движки кешируются по
паре `(режим, язык)`; размер кеша ограничен `RAGU_API_ENGINE_CACHE_SIZE`, так
как язык выбирает клиент.

Значение подставляется в промпт, поэтому ограничено формой обычного названия
языка (`^[A-Za-z][A-Za-z \-]*$`, 2–32 символа), а не принимается свободным
текстом.

### Реранжирование

`rerank` (по умолчанию `true`) просит использовать реранкер развёртывания;
`rerank_top_k` внутри `params` задаёт, сколько результатов оставить после него.

Сервис никогда не создаёт реранкер сам — на CPU-развёртывании модель работает в
отдельном контейнере, — поэтому его передают в `create_app(reranker=...)`. Если
реранкера нет, `rerank` ничего не делает.

Отказ реранкера или превышение `RAGU_API_RERANK_TIMEOUT` стоит качества
ранжирования, а не ответа: порядок ретривала сохраняется, `engines.rerank_error`
сообщает причину, `engines.reranked` — `false`, `engines.degraded` — `true`. Без
этого сбой реранкера превращался бы в 500 для запроса, на который движки могли
ответить.

### Ретривал без генерации

`POST /v1/search/{mode}/retrieve` возвращает только контекст — для клиентов,
которые генерируют ответ сами. Он не принимает `use_query_plan`:
`QueryPlanEngine.batch_search` делегирует напрямую вложенному движку и никакого
планирования не выполняет, так что приём этого флага обещал бы декомпозицию,
которой не будет. Передача флага даёт `400`.

### Батчи

`POST /v1/search/{mode}/batch` принимает `queries` и отвечает на все за один
проход. Именно здесь движки окупаются: ретривал разделяется на весь список, а
при плане запроса независимые подзапросы **разных** верхнеуровневых запросов
отвечаются в одном дочернем батче.

Запрос, который ничего не наретривил, несёт `error` вместо ответа — один пустой
запрос не роняет батч. Размер списка ограничен `RAGU_API_MAX_BATCH_SIZE`.

```json
{"mode": "naive", "used_query_plan": true, "engines": {"...": "..."},
 "results": [{"query": "...", "answer": "...", "sources": [], "subqueries": []}]}
```

### Стриминг

`POST /v1/search/{mode}/stream` отдаёт `text/event-stream`: одно событие `meta`
с ретривалом и отчётом о движках, затем события `delta` с текстом, затем `done`
с финальным отчётом. Отказ после отправки заголовков приходит событием `error`;
проверка возможностей выполняется **до** начала ответа, поэтому недоступный
режим по-прежнему отбивается статус-кодом.

### Что реально отработало

Каждый ответ несёт `engines`:

```json
{"requested": "mix", "used": "MixSearchEngine", "query_plan": true,
 "degraded": true,
 "children": [{"engine": "LocalSearchEngine", "mode": "local", "ok": false,
               "error": "RuntimeError: entity vector store unreachable"},
              {"engine": "NaiveSearchEngine", "mode": "naive", "ok": true,
               "error": null}]}
```

`MixSearchEngine` работает с `allow_partial_failures=True` и выбрасывает
упавший дочерний движок, поэтому без этого «граф и чанки» было бы неотличимо от
«только чанки». `degraded` истинно, когда какой-то дочерний движок не внёс
вклад.

## Health и readiness

| Маршрут | Статус | Назначение |
|---|---|---|
| `GET /health` | всегда `200` | Отладочный обзор; готовность — в теле ответа |
| `GET /health/live` | всегда `200` | Проба liveness: процесс жив |
| `GET /health/ready` | `200` / `503` + `Retry-After` | Проба readiness: поиск можно обслуживать |

Загрузка большого графа занимает минуты, поэтому liveness и readiness
разделены: оркестратор не должен перезапускать сервис, который ещё стартует.

```json
{
  "status": "ok",
  "graph_loaded": true,
  "stats": {"entities": 128400, "relations": 170233, "chunks": 9812, "community_summaries": 341},
  "error": null
}
```

`stats` измеряется один раз при старте, и именно из него выводится
`graph_loaded`: он означает «в графе есть что читать хотя бы одному режиму
поиска», а не «объект сконструирован». Если старт провалился, `error` несёт
причину, и та же причина попадает в тело `503` любого поиска — диагностика не
остаётся закопанной в логах.

## Ошибки

У всех ошибок один конверт:

```json
{"error": {"code": "CAPABILITY_UNAVAILABLE", "mode": "global",
           "missing_capability": "community_summaries", "message": "..."}}
```

| Статус | Код | Ситуация |
|---|---|---|
| 400 | `INVALID_REQUEST` | пустой `query`, неверный тип поля, неизвестное поле |
| 404 | `GRAPH_NOT_FOUND` | путь называет граф, которого нет в конфигурации |
| 404 | `JOB_NOT_FOUND` | задачи с таким id в процессе нет |
| 409 | `GRAPH_BUSY` | граф собирается; несёт `Retry-After` |
| 409 | `CAPABILITY_UNAVAILABLE` | граф не умеет этот режим, либо запрос ничего не нашёл |
| 503 | `SERVICE_NOT_READY` | граф не загружен или старт провалился; несёт `Retry-After` |
| 500 | `INTERNAL_ERROR` | LLM недоступна, таймаут эмбеддера, отказ движка |

`409` существует, чтобы клиент мог среагировать сам: граф, построенный без
саммари сообществ, не ответит на глобальный поиск, а построенный без извлечения
сущностей — на локальный. `missing_capability` различает два случая `409`:

- **названа** (`community_summaries`, `entity_graph`, `vector_index`) — в графе
  нет того, что читает этот режим. Определяется по стартовому измерению,
  поэтому генерация на это не тратится.
- **`null`** — режим граф поддерживает, но конкретно этот запрос не нашёл
  свидетельств. Движки уверенно отвечают и на пустом контексте, поэтому ответ,
  построенный ни на чём, сообщается как ошибка, а не возвращается.

Ответы `500` никогда не цитируют исходное исключение: ошибки движка и
LLM-клиента регулярно содержат URL эндпоинта и куски запроса. Детали уходят в
лог сервиса.

## Проектные решения

**Почему в запрос встроен класс параметров движка.** `QueryPlanEngine`
передаёт `params` вложенному движку без изменений, поэтому опции запроса обязаны
путешествовать объектом параметров, а не аргументами конструктора.
Переиспользование тех же классов как схемы запроса означает, что параметр,
добавленный выше по стеку, доступен по HTTP без правок здесь — а
переименованный выше меняет контракт на проводе, и это цена того, что их не
переписывают заново.

**Почему возможности измеряются, а не угадываются.** Движки отвечают из любого
собранного контекста, включая пустой. Подсчёт хранилищ сущностей, чанков и
саммари один раз при старте — это то, что позволяет отказать в неподдерживаемом
режиме до оплаты генерации, и то, что делает `graph_loaded` правдивым.

**Почему конвертация диспетчеризуется по типам результата.** Движки — часть
этого же пакета и версионируются вместе с ним, поэтому `ragu/api/mapping.py`
диспетчеризуется по конкретным классам `*SearchResult`, а не прощупывает
атрибуты. Тип результата, сменивший форму, тогда падает громко, а не порождает
пустой список источников, который сервис объявил бы отсутствующей возможностью.

**Почему ручки стоимости — на стороне сервера.** Один глобальный поиск стоит
N+1 LLM-вызовов на N уцелевших сообществ. `RAGU_API_MIN_CLUSTER_SIZE_FLOOR` —
единственный ограничитель этого, как `RAGU_API_MAX_TOP_K` ограничивает ширину
ретривала; оба применяются в базовом классе бэкенда, поэтому любой бэкенд
соблюдает их одинаково.

## Логирование

`python -m ragu.api` вызывает `configure_logging` (`ragu/api/logging_setup.py`),
который ставит перехватывающий handler на корень stdlib-логирования и
переизлучает каждую запись через loguru. Uvicorn, httpx и сам сервис после
этого делят один синк и один формат с движками, поэтому один запрос
прослеживается от начала до конца. `--log-level` задаёт уровень этого синка.

Импорт `create_app` в другой сервер (gunicorn, уже настроенный uvicorn) чужое
логирование не трогает — вызовите `configure_logging` сами, если нужно такое же
поведение.

## Текущие ограничения

Известные пробелы — перечислены, чтобы их не приняли за недосмотр:

- **Нет CRUD по графу.** Сущности, связи, чанки и сообщества нельзя читать и
  править по HTTP.
- **Нет аутентификации, квот и метрик.** Каждый запрос стоит LLM-вызовов;
  ставьте сервис за шлюз, который аутентифицирует и ограничивает частоту.
