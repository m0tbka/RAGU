name: Test Package

on:
  push:
    branches: [ main, dev ]
  # Long-lived integration branches are merge targets too, and a pull request is
  # only gated on the branches named here: without them a PR into feature/*
  # lands with no run at all.
  pull_request:
    branches: [ main, dev, 'feature/**' ]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: pip install uv

      - name: Install package with test dependencies
        # The `api` extra is required: tests/api/ opens with
        # pytest.importorskip("fastapi") and is silently skipped without it.
        run: |
          uv pip install --system -e ".[api,test]"

      - name: Test import
        run: python -c "import ragu; print(ragu.__version__)"

      - name: Run tests
        run: |
          pytest tests/

      # tests/api/ opens with pytest.importorskip("fastapi"), so a resolution
      # that drops the extra leaves the whole service suite skipped and the job
      # green. Same guard as the Neo4j job below, for the same reason.
      - name: Fail if the service suite silently skipped
        run: |
          pytest tests/api -q --no-header --no-cov -p no:cacheprovider | tee /tmp/api-run.txt
          grep -qE '[0-9]+ passed' /tmp/api-run.txt
          if grep -qE '[0-9]+ skipped' /tmp/api-run.txt; then
            echo "::error::tests/api was skipped - the 'api' extra did not install"
            exit 1
          fi

  # Storage backends that need a server are exercised here. Without this job the
  # Neo4j adapter has no automated coverage at all: its tests skip themselves
  # when no server is reachable, so the matrix job above stays green either way.
  neo4j-integration:
    name: Neo4j integration
    runs-on: ubuntu-latest

    services:
      neo4j:
        image: neo4j:5-community
        env:
          NEO4J_AUTH: neo4j/testpassword
        ports:
          - 7687:7687
        options: >-
          --health-cmd "cypher-shell -u neo4j -p testpassword 'RETURN 1'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 15

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        run: pip install uv

      - name: Install package with test dependencies
        run: |
          uv pip install --system -e ".[test]"

      - name: Run graph storage tests against a live Neo4j
        env:
          NEO4J_URI: bolt://localhost:7687
          NEO4J_USER: neo4j
          NEO4J_PASSWORD: testpassword
        run: |
          pytest tests/storage/test_neo4j_adapter.py tests/storage/test_graph_contract.py

      - name: Fail if the suite silently skipped
        env:
          NEO4J_URI: bolt://localhost:7687
          NEO4J_USER: neo4j
          NEO4J_PASSWORD: testpassword
        run: |
          pytest tests/storage/test_graph_contract.py -k neo4j -q --no-header -p no:cacheprovider \
            | tee /tmp/neo4j-run.txt
          grep -qE '[0-9]+ passed' /tmp/neo4j-run.txt
          if grep -qE '[0-9]+ skipped' /tmp/neo4j-run.txt; then
            echo "::error::Neo4j contract tests were skipped - the server was not reachable"
            exit 1
          fi

  mirror:
    name: Mirror to GitVerse
    needs: [test, neo4j-integration]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push mirror to GitVerse
        env:
          GITVERSE_USERNAME: ${{ secrets.GITVERSE_USERNAME }}
          GITVERSE_TOKEN: ${{ secrets.GITVERSE_TOKEN }}
          GITVERSE_REPO_URL: ${{ secrets.GITVERSE_REPO_URL }}
        run: |
          git remote add gitverse https://${GITVERSE_USERNAME}:${GITVERSE_TOKEN}@${GITVERSE_REPO_URL#https://}
          git push gitverse \
            +refs/heads/*:refs/heads/* \
            +refs/tags/*:refs/tags/*

